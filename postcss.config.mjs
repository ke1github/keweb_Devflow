/** @type {import('postcss-load-config').Config} */
export default {
  plugins: {
    tailwindcss: {}, // Tailwind plugin for processing
    autoprefixer: {}, // Ensures browser compatibility
  },
};
